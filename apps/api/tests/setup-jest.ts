// if (process.env.DEBUGGER_ATTACHED === "jest") {
    //     jest.setTimeout(6e5); // 10 minutes timeouts when debugger is attached
    //   } else {
    //     jest.setTimeout(2e4);
    //   }
      